import * as handlers from "./handler";

import {
  padAddress,
  createAddress,
  createChecksumAddress,
  toChecksumAddress,
  NetworkManager,
} from "./utils";

export {
  handlers,
  padAddress,
  createAddress,
  createChecksumAddress,
  toChecksumAddress,
  NetworkManager,
};

export default {
  handlers,
  padAddress,
  createAddress,
  createChecksumAddress,
  toChecksumAddress,
  NetworkManager,
};
