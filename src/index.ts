import * as handlers from "./handler";

import {
  padAddress,
  createAddress,
  createChecksumAddress,
  toChecksumAddress,
  NetworkManager,
  ProviderCache,
  CachedContract,
} from "./utils";

export {
  handlers,
  padAddress,
  createAddress,
  createChecksumAddress,
  toChecksumAddress,
  NetworkManager,
  ProviderCache,
  CachedContract,
};

export default {
  handlers,
  padAddress,
  createAddress,
  createChecksumAddress,
  toChecksumAddress,
  NetworkManager,
  ProviderCache,
  CachedContract,
};
